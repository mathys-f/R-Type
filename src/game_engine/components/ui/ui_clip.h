#pragma once

namespace engn {

namespace cpnt {

// Defines a clipping rectangle for UI rendering (values are in UI percent space).
struct UIClip {
    float x;
    float y;
    float w;
    float h;
};

} // namespace cpnt

} // namespace engn
