name: Build R-Type

on:
  pull_request:
    branches: [ main, dev ]

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Debug, Release]

    runs-on: ${{ matrix.os }}
    timeout-minutes: 30  # Add explicit timeout

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup C++ environment
      uses: aminya/setup-cpp@v1
      with:
        compiler: gcc
        cmake: true
        ninja: true

    - name: Cache CPM dependencies
      uses: actions/cache@v4
      with:
        path: |
          build/_deps
          ~/.cache/CPM
        key: ${{ runner.os }}-cpm-${{ hashFiles('CMakeLists.txt', 'src/**/CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-cpm-

    - name: Cache CMake build
      uses: actions/cache@v4
      with:
        path: |
          build
        key: ${{ runner.os }}-build-${{ matrix.build_type }}-${{ hashFiles('CMakeLists.txt', 'src/**/*') }}
        restore-keys: |
          ${{ runner.os }}-build-${{ matrix.build_type }}-
          ${{ runner.os }}-build-

    - name: Install Linux dependencies
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential \
          libx11-dev \
          libxrandr-dev \
          libxinerama-dev \
          libxcursor-dev \
          libxi-dev \
          libgl1-mesa-dev \
          libasound2-dev \
          mesa-common-dev

    - name: Create build directory
      run: mkdir -p build

    - name: Configure CMake with timeout protection
      timeout-minutes: 15  # Add step-level timeout
      run: |
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} -G Ninja \
          -DCPM_SOURCE_CACHE=~/.cache/CPM \
          -DCMAKE_VERBOSE_MAKEFILE=OFF

    - name: Build project
      timeout-minutes: 10
      run: |
        cd build
        cmake --build . --config ${{ matrix.build_type }} --parallel 2

    - name: Upload Linux artifacts
      if: runner.os == 'Linux'
      uses: actions/upload-artifact@v4
      with:
        name: r-type-linux-${{ matrix.build_type }}
        path: |
          build/bin/${{ matrix.build_type }}/r-type_client
          build/bin/${{ matrix.build_type }}/r-type_server

    - name: Upload Windows artifacts
      if: runner.os == 'Windows'
      uses: actions/upload-artifact@v4
      with:
        name: r-type-windows-${{ matrix.build_type }}
        path: |
          build/bin/${{ matrix.build_type }}/r-type_client.exe
          build/bin/${{ matrix.build_type }}/r-type_server.exe

  test:
    needs: build
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Debug, Release]

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Download artifacts (Linux)
      if: runner.os == 'Linux'
      uses: actions/download-artifact@v4
      with:
        name: r-type-linux-${{ matrix.build_type }}
        path: ./artifacts

    - name: Download artifacts (Windows)
      if: runner.os == 'Windows'
      uses: actions/download-artifact@v4
      with:
        name: r-type-windows-${{ matrix.build_type }}
        path: ./artifacts

    - name: Make executables executable (Linux)
      if: runner.os == 'Linux'
      run: |
        chmod +x ./artifacts/r-type_client
        chmod +x ./artifacts/r-type_server

    - name: Test executables exist
      run: |
        ls -la ./artifacts/

    # Add more specific tests here as needed
    # For example, you could run unit tests or basic functionality tests
